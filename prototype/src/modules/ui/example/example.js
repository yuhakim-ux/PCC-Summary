import { LightningElement, api } from 'lwc';

/**
 * Example ui component. Use as <ui-example> in templates.
 * Duplicate this folder and rename for new ui components (e.g. ui-card, ui-modal).
 */
export default class Example extends LightningElement {
    @api title = 'Example';
}
